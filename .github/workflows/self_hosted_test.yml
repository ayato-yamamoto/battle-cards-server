name: Deploy Product Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy
        run: |
          whoami



          # cd /var/www/battle-cards-server

          # git fetch origin
          # git reset --hard origin/main