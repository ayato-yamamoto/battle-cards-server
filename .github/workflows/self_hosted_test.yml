name: Deploy Product Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy
        run: |
          cd /var/www/battle-cards-server

          git fetch origin
          git reset --hard origin/main

  # test:
  #   runs-on: self-hosted

  #   steps:
  #     - run: echo "Hello Runner"
  #     - run: whoami
  #     - run: pwd